# Local environment variables (sensitive)
.env

# Qdrant local vector DB — payload embeds REAL DMX catalog + real chat history (NDA).
# Never commit; regenerate locally via antigravity/vector_db.py.
qdrant_db/

# Python build and virtualenv files
__pycache__/
*.py[cod]
*$py.class
.venv/
.venv311/
venv/
ENV/
env/
build/
dist/

# Editor files
.vscode/
.idea/
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db

# Workspace agent files we don't want to commit (optional)
# root-anchored so antigravity/.agents/ (app config, loaded by core.py) stays committable
/.agents/
temp_*/
work-tmp/

# BTC hackathon data (NDA) - do not commit
data/raw/
data/processed/btc_*
data/processed/dmx_*
artifacts/btc_*
artifacts/dmx_*

# DMX organizer resources (NDA — "đừng share bên ngoài") - do not commit
DMX_product.zip
products_detail.*
*chat_history*.json
35sample_chat_history*.json
newnotefromref.md
chat_luong_phuc_vu.md
chinh_sach_*.md
dieu-khoang-su-dung.md
noi_quy_cua_hang.md
Insights for PM.docx
DMX_AI_Advisor_PM_Brief.docx
Dien-May-Xanh-Enterprise-Problem-Brief.md
AI Product Comparison Advisor Based.txt
data/dmx/

# Large vendored libs / assets (local only)
libs/
*.zip
*.pptx

# Teammate backup snapshot (5000+ files) — not part of this repo's build
backend_final-main/

# Agent/workspace + local noise (keep Source Control panel clean, prevent leaks)
.claude/
memory.md

# Demo catalog = SYNTHETIC ("giả lập") aircon records, generated by
# scripts/build_demo_catalog.py. NO real DMX SKU/price/URL — brief E2 line 142 requires
# demo data be giả lập/anonymize. Safe to commit + deploy publicly; it powers the real
# pipeline on the live URL without exposing NDA data. (If you ever drop REAL records in
# here, re-add the ignore.)
!demo_catalog/

# Secrets / local briefs / binaries (never commit)
FPT Factory API Key.txt
*.png
# ...except frontend UI assets — these must ship to the repo/deploy
!frontend/img/**
!frontend/img/*.png
!frontend/favicon.ico
!frontend/icon.png
!frontend/icon.svg
*.docx
*.docx.pdf
*.pdf

.vercel
